# 🐳 Employee Database System with Docker

Система управления данными сотрудников с Docker-контейнеризацией.

##  Особенности
- 🐳 Полная Docker-контейнеризация (приложение + БД)
-  Оптимизированная массовая вставка 1M+ записей
-  Быстрый поиск по критериям
-  Встроенное логирование

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/employee-db-system.git
cd employee-db-system

# Запуск программы 
# Собрать и запустить все сервисы (приложение + БД)
docker-compose up --build

# Или в фоновом режиме:
docker-compose up -d --build
# Шаг 1: Запустить только базу данных
docker-compose up -d db

# Шаг 2: Собрать приложение
docker-compose build app

# Шаг 3: Запустить команды
docker-compose run --rm app ./app [номер_режима] [аргументы]

1. Создание таблицы с полями справочника сотрудников, представляющими "Фамилию Имя Отчество", "дату рождения", "пол".
2. Создание записи справочника сотрудника.
3. Вывод всех строк справочника сотрудников, с уникальным значением ФИО+дата, отсортированным по ФИО.
4. Заполнение автоматически 1000000 строк справочника сотрудников. Добавляет заполнение автоматически 100 строк в которых пол мужской и Фамилия начинается с "F".
5. Результат выборки из таблицы по критерию: пол мужской, Фамилия начинается с "F". Также проводит замер времени выполнения.
# Примеры:
docker-compose run --rm app ./app 1 
docker-compose run --rm app ./app 2 Ivanov Petr Sergeevich 2009-07-12 Male 
docker-compose run --rm app ./app 3                                         
docker-compose run --rm app ./app 4                                         
docker-compose run --rm app ./app 5     
